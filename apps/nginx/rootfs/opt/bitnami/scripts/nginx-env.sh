#!/bin/bash
# SPDX-License-Identifier: Apache-2.0
# shellcheck shell=bash

export BITNAMI_ROOT_DIR="${BITNAMI_ROOT_DIR:-/opt/bitnami}"
export BITNAMI_VOLUME_DIR="${BITNAMI_VOLUME_DIR:-/bitnami}"
export NGINX_BASE_DIR="${NGINX_BASE_DIR:-${BITNAMI_ROOT_DIR}/nginx}"
export NGINX_BIN_DIR="${NGINX_BIN_DIR:-${NGINX_BASE_DIR}/sbin}"
export NGINX_SBIN_DIR="${NGINX_SBIN_DIR:-${NGINX_BIN_DIR}}"
export NGINX_CONF_DIR="${NGINX_CONF_DIR:-${NGINX_BASE_DIR}/conf}"
export NGINX_DEFAULT_CONF_DIR="${NGINX_DEFAULT_CONF_DIR:-${NGINX_BASE_DIR}/conf.default}"
export NGINX_CONF_FILE="${NGINX_CONF_FILE:-${NGINX_CONF_DIR}/nginx.conf}"
export NGINX_SERVER_BLOCKS_DIR="${NGINX_SERVER_BLOCKS_DIR:-${NGINX_CONF_DIR}/server_blocks}"
export NGINX_STREAM_SERVER_BLOCKS_DIR="${NGINX_STREAM_SERVER_BLOCKS_DIR:-${NGINX_CONF_DIR}/stream_server_blocks}"
export NGINX_LOGS_DIR="${NGINX_LOGS_DIR:-${NGINX_BASE_DIR}/logs}"
export NGINX_TMP_DIR="${NGINX_TMP_DIR:-${NGINX_BASE_DIR}/tmp}"
export NGINX_PID_FILE="${NGINX_PID_FILE:-${NGINX_TMP_DIR}/nginx.pid}"
export NGINX_HTTP_PORT_NUMBER="${NGINX_HTTP_PORT_NUMBER:-8080}"
export NGINX_HTTPS_PORT_NUMBER="${NGINX_HTTPS_PORT_NUMBER:-8443}"
export NGINX_ENABLE_STREAM="${NGINX_ENABLE_STREAM:-no}"
export NGINX_ENABLE_ABSOLUTE_REDIRECT="${NGINX_ENABLE_ABSOLUTE_REDIRECT:-no}"
export NGINX_ENABLE_PORT_IN_REDIRECT="${NGINX_ENABLE_PORT_IN_REDIRECT:-no}"
export NGINX_WORKER_PROCESSES="${NGINX_WORKER_PROCESSES:-auto}"
export NGINX_DAEMON_USER="${NGINX_DAEMON_USER:-daemon}"
export NGINX_DAEMON_GROUP="${NGINX_DAEMON_GROUP:-daemon}"
export NSS_WRAPPER_LIB="${NSS_WRAPPER_LIB:-/opt/bitnami/common/lib/libnss_wrapper.so}"
